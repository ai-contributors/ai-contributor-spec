# Copy to .env and fill in real values. NEVER commit the filled-in .env file.
# Placeholders only — see AI-CONTRIBUTOR-SPECIFICATION.md §5.

DATABASE_URL=postgres://<db-user>:<db-password>@<db-host>:5432/<db-name>
LOG_LEVEL=info
NODE_ENV=development
